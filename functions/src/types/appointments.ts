export type BaseAppointment = {
    courseId: string;
    date: string;
    momsAttended: string[];   
}

export type Appointment = {
    id: string;
    courseId: string;
    date: string;
    momsAttended: string[];  
}