export type BaseMom = {
    firstName: string;
    lastName: string;
    billsPayed: boolean;
    courses: string[];
    attendance: number;    
}

export type Mom = {
    id: string;
    firstName: string;
    lastName: string;
    billsPayed: boolean;
    courses: string[];
    attendance: number;
}
